{
  "name": "amplify-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc && node --loader ts-node/esm --experimental-specifier-resolution=node backend.ts",
    "sandbox": "amplify sandbox",
    "deploy-branch": "amplify deploy --branch",
    "deploy-prod": "amplify deploy --prod"
  },
  "devDependencies": {
    "@aws-amplify/backend": "^1.0.0",
    "@aws-amplify/backend-cli": "^1.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.0.0"
  }
}